const widgetKeys = {
  TASKS_NUMBER: 'tasksNumber',
};

const widgetNames = {
  [widgetKeys.TASKS_NUMBER]: 'Overall tasks amount',
};

const defaultActiveWidgets = [
  widgetKeys.TASKS_NUMBER,
];

const widgetsList = [
  widgetKeys.TASKS_NUMBER,
];

export {
  widgetKeys,
  widgetNames,
  defaultActiveWidgets,
  widgetsList,
};
